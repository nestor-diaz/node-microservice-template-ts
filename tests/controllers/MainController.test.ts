import { Container } from 'typedi';
import { MainService } from '../../src/services/MainService';
import { ServiceInteface } from '../../src/services/ServiceInterface';

describe('MainController', () => {
  beforeEach(() => {
  });

  test('should have a response when calling GET method', () => {

  });
});
